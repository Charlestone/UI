Problemas no resueltos:

-No hemos podido implementar los input con type date y month para Firefox.

-Los input para el número de la tarjeta y el código de seguridad son type text en vez de number porque siendo number no hemos conseguido que maxlength se aplique de manera correcta.
